@model Supporter.Entity.AnaUrun

@{
    ViewBag.Title = "Edit";
}

<h2 data-aos="zoom-in-right">Ana Ürün Düzenle</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal" data-aos="zoom-in">
        <h4>Ana Ürün</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.Id)

        <div class="form-group">
            @Html.LabelFor(model => model.AnaUrunAdi, htmlAttributes: new { @class = "control-label col-md-2  hvr-buzz" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.AnaUrunAdi, new { htmlAttributes = new { @class = "form-control hvr-grow" } })
                @Html.ValidationMessageFor(model => model.AnaUrunAdi, "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Güncelle" class="btn btn-default hvr-grow" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Listele", "Index")
</div>
