@model IEnumerable<Supporter.Entity.Versiyon>

@{
    ViewBag.Title = "Index";
}

<h2 data-aos="zoom-in-right" class="hvr-buzz">Versiyonlar</h2>

<p>
    @Html.ActionLink("Yeni Versiyon Ekle", "Create")
</p>
<table class="table" data-aos="zoom-in-down">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.VersiyonAdi)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.VersiyonAdi)
        </td>
        <td>
            @Html.ActionLink("Düzenle", "Edit", new { id=item.Id }) |
            @Html.ActionLink("İncele", "Details", new { id=item.Id }) |
            @Html.ActionLink("Sil", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
