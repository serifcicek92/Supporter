@model IEnumerable<Supporter.Entity.Modul>

@{
    ViewBag.Title = "Index";
}

<h2 data-aos="zoom-in-left">Modüller</h2>

<p>
    @Html.ActionLink("Yeni Modül Oluştur", "Create")
</p>
<table class="table"  data-aos="zoom-in">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ModulAdi)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ModulAdi)
        </td>
        <td>
            @Html.ActionLink("Düzenle", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Görüntüle", "Details", new { id=item.Id }) |
            @Html.ActionLink("Sil", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
